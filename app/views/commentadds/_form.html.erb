<%= form_with(model: commentadd, local: true) do |form| %>
  <% if commentadd.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(commentadd.errors.count, "error") %> prohibited this commentadd from being saved:</h2>

      <ul>
      <% commentadd.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
